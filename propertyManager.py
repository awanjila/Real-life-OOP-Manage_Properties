class Property(object):
	def __init__(self):
		self.name=name;
		self.size=size;
		self.number=number
		self.value=value
	def buying:
		pass
    def __str__(self):
     return "%s is a %s" % (self.name, self.size, self.number, self.value)
